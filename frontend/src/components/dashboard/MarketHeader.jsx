// Not implemented — blocked on a live broker feed for NIFTY/BANKNIFTY/VIX per frontend.md's
// "Future work: live scanner dashboard" section. Scoped now so the directory layout matches
// the spec; do not wire this up until that data source exists.
export default function MarketHeader() {
  return null
}
