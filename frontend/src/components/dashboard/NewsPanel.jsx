// Not implemented — blocked on the news engine, which does not exist yet per
// frontend.md's "Future work: live scanner dashboard" section and engine.md's v1 limitations.
export default function NewsPanel() {
  return null
}
