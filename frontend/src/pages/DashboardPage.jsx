// Future: live scanner dashboard (section 40 layout), gated on the candle-builder/live-detection
// engines per frontend.md's "Future work" section. Route exists but is not linked from v1 nav.
export default function DashboardPage() {
  return (
    <div className="flex min-h-[50vh] items-center justify-center">
      <p className="text-slate-500">Live scanner dashboard — coming soon.</p>
    </div>
  )
}
